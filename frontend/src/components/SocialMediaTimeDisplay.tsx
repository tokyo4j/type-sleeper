import React from "react";

const SocialMediaTimeDisplay = () => {
  return (
    <div>
      <h2 className="text-3xl font-bold mb-4">SNS等の使用時間</h2>
      {/* ここにSNS等の使用時間を表示するコンポーネントを追加します */}
    </div>
  );
};

export default SocialMediaTimeDisplay;
